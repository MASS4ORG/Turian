# Turian Editor (`editor/`)

The **editor** module provides tooling infrastructure between the engine and the studio GUI. It is a library (no mutable state) of scan, serialize, and I/O operations. It re-exports `engine` and declares `name`/`version` for identification.
