//! Turian logo, rasterized from `docs/turian-logo-1.svg`. Shared by the
//! window/taskbar icon (`Main.zig`) and the About dialog (`main-window/MenuBar.zig`).
pub const png = @embedFile("assets/icon.png");
